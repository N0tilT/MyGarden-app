abstract class ICommonEntity<TModel> {
  // ignore: avoid_unused_constructor_parameters
  factory ICommonEntity.fromModel(TModel model) =>
      throw UnimplementedError('fromModel() not implemented');
}
